import 'package:flutter_test/flutter_test.dart';

void main() {
  // 본격적인 위젯 테스트는 각 feature 구현 단계에서 작성한다.
  // 현재는 lint 통과 및 test 인프라 유지용 placeholder 다.
  test('setup placeholder', () {
    expect(true, isTrue);
  });
}