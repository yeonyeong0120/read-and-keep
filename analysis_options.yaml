# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

# Riverpod 3.x lint is implemented as an analysis_server_plugin (NOT custom_lint).
# It is enabled via the top-level `plugins:` key. See:
# https://github.com/rrousselgit/riverpod/blob/master/packages/riverpod_lint/README.md
plugins:
  riverpod_lint: ^3.1.3

analyzer:
  # build_runner 가 생성한 파일은 분석 대상에서 제외한다.
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

  errors:
    missing_required_param: error
    missing_return: error
    # Riverpod/Hive 코드 생성 시 흔히 발생하지만 무해하므로 무시한다.
    invalid_annotation_target: ignore

  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  # flutter_lints 기본 셋 위에 아래 규칙들을 명시적으로 추가한다.
  rules:
    - prefer_single_quotes
    - prefer_const_constructors
    - prefer_const_literals_to_create_immutables
    - avoid_print # debugPrint 사용 권장
    - use_super_parameters
    - require_trailing_commas
    - unawaited_futures # 비동기 누락 감지

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
